# Copy to .env.local and adjust values.

# Browser + bundled client: required for production (Vercel). No trailing slash.
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# Next.js API routes (server-only proxy to Express). Falls back to NEXT_PUBLIC_* if unset.
BACKEND_URL=http://localhost:5000
